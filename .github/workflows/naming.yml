name: Naming
run-name: Run by @${{github.actor}}
on: push
jobs:
    print:
        name: Super useful job
        runs-on: ubuntu-latest
        steps:
            - name: Amazing Script
              run: echo "Hello"
